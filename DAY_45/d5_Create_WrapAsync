// util means utility fun- extra fun jo humne banaya hai for error handling
// directly export kar sakte hai
module.exports = (fn) => {
    return (req,res,next)=>{
        fn(req,res,next).catch(next);
    };
};
